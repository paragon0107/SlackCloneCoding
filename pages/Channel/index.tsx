import React from 'react';
import { Container, Header } from '@pages/Channel/styles';

const Channel = ()=>{
  console.log("Channel")

  return (
    <Container>
      <Header>
        채널!!~!!
      </Header>
    </Container>
  );
};

export default Channel;